import 'package:flutter/cupertino.dart';

const defaultPadding = EdgeInsets.only(
  left: 20,
  right: 20,
);

const frequentlyUsedSizedHeight = SizedBox(
  height: 20,
);

const frequentlyUsedSizedWidth = SizedBox(
  width: 20,
);
